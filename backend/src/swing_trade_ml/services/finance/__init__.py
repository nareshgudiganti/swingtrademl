"""Personal finance module: statement parsing, categorization, analytics.

Ported from a standalone Streamlit app (bank/PhonePe statement parsing +
keyword categorization) into this app's own storage and API layer. See
`ingestion.ingest_statement` for the orchestration entry point.
"""
